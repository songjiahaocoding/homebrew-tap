# homebrew-tap

Homebrew tap for [fe](https://github.com/songjiahaocoding/fe).

After the release workflow publishes binary assets and updates this tap, install with:

```sh
brew install songjiahaocoding/tap/fe
```
